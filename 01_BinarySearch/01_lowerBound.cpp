// the lowwer bound algorithm finds the first or smallest index in array where value at the index in sorted array is either equal to or more than given target ie x.
// lower bound is index where arr[index] >= x. if such index doesn't exist, the algorithm returns size of array ie n.
